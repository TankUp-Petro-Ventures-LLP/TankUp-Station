import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Tankup-station',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
